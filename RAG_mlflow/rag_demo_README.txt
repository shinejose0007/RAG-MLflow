RAG Demo — Embeddings + FAISS retrieval

File: rag_demo_notebook.ipynb

This notebook demonstrates:
- Creating embeddings with sentence-transformers (all-MiniLM-L6-v2)
- Indexing with faiss (IndexFlatL2)
- Simple retrieval and prompt composition for RAG

Run instructions:
1. pip install -U sentence-transformers faiss-cpu numpy
2. Open and run the notebook in Jupyter or Colab.

